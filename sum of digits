#include <stdio.h>

int sum_digits(int n) {
    int sum = 0;
    while (n > 0) {
        sum += n % 10;
        n /= 10;
    }
    return sum;
}

int main() {
    int num;
    printf("Enter a no.: ");
    scanf("%d", &num);
    printf("Sum of digits = %d\n", sum_digits(num));
    return 0;
}
